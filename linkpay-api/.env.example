# ============================================
# LinkPay Backend Environment Variables
# ============================================
# Copy this file to .env and fill in your values
# NEVER commit .env to git

# App
NODE_ENV=development
PORT=3000
FRONTEND_URL=http://localhost:5173

# Supabase
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
DATABASE_URL=postgresql://postgres:password@db.your-project.supabase.co:5432/postgres

# JWT
JWT_SECRET=change-me-in-production
JWT_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

# PSP Configuration
# PSP_PROVIDER=mock (default demo/dev mode) | cinetpay (real Mobile Money RDC + carte)
PSP_PROVIDER=mock
PSP_SANDBOX=true
PSP_PUBLIC_KEY=
PSP_SECRET_KEY=
PSP_WEBHOOK_SECRET=
PSP_BASE_URL=

# CinetPay (cinetpay.com) — Mobile Money RDC (Orange/Airtel/M-Pesa) + carte
# Obtenir sur cinetpay.com après inscription (mode test disponible avant validation KYC)
CINETPAY_API_KEY=
CINETPAY_SITE_ID=
# Réutilise PSP_WEBHOOK_SECRET ci-dessus comme clé secrète HMAC de notification CinetPay

# Security
RATE_LIMIT_TTL=60
RATE_LIMIT_MAX=100
WEBHOOK_REPLAY_WINDOW=300

# Logging
LOG_LEVEL=debug
