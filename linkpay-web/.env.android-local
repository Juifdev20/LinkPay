# LinkPay Android (Capacitor) build — overrides .env for `npm run build:android:local`.
# Talks to a NestJS backend running locally on the PC (`npm run start:dev` in
# linkpay-api) instead of the deployed Render API — requires `adb reverse
# tcp:3000 tcp:3000` first so the phone's `localhost:3000` tunnels to the PC
# over the USB connection. Use this to test backend changes on the phone
# without waiting for (or triggering) a Render deploy. `.env.android` (the
# Render-pointed build) is untouched — this is purely additive.

VITE_API_URL=http://localhost:3000/api/v1

# Supabase Realtime (anon key — safe to expose, protected by RLS)
VITE_SUPABASE_URL=https://gkvumrzssyyabhxdovmw.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImdrdnVtcnpzc3l5YWJoeGRvdm13Iiwicm9sZSI6ImFub24iLCJpYXQiOjE3ODc5OTQzOTQsImV4cCI6MjEwMzU3MDM5NH0.ErKsvz5ROH-biQsTGEhsX9r8kn4uKY7Md1Zc_HNJ9Uc

# Web Push (VAPID) — must match VAPID_PUBLIC_KEY in linkpay-api/.env
VITE_VAPID_PUBLIC_KEY=BJf8827oHoahVxMNho9OmimhL3wTC6FdejQmyW4xUhAfIeucG3FjKDpYzDY8TPsPd3pyOsy6jU82OIlE7IiKFzY
